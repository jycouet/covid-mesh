# Write your query or mutation here
query getData {
  case(countryRegion: "France") {
    confirmed
    deaths
    recovered
    countryRegion
  }
  population(country: "France") {
    records {
      fields {
        value
        country_name
      }
    }
  }
}
